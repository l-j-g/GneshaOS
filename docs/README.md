# Documentation

These notes cover installation, parameters, host discovery, and safe build
workflows. Build a discovered host with
`nixos-rebuild build --flake .#<host-directory-name>`; activate or install only
when following the machine-specific operational runbook.
