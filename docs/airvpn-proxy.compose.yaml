# Merge with /home/lg/.config/containers/arr/docker-compose.yml; not standalone.
# https://github.com/qdm12/gluetun-wiki/blob/main/setup/options/http-proxy.md
services:
  gluetun:
    environment:
      HTTPPROXY: "on"
    ports:
      - "127.0.0.1:8888:8888/tcp"
