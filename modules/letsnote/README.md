# Let's Note module

This custom module provides EC features, power controls, fan behavior, and
JIS-key handling for supported Panasonic Let's Note hardware. Its options
are hardware-dependent: verify the target model and kernel interfaces before
enabling them, especially `fanControl` and CPU power settings.

Host modules enable the options for a specific machine. Do not put disk UUIDs
or other generated hardware configuration here.
